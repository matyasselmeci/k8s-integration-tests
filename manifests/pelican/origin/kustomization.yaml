apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - config.yaml
  - service.yaml
  - deployment.yaml

configMapGenerator:
  - name: origin-test-scripts
    files:
      - make_testfiles.sh

generatorOptions:
  disableNameSuffixHash: true
